export const metadata = {
  title: 'Login - TransportApp',
  description: 'Inicia sesión en tu cuenta',
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
